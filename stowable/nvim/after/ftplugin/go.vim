" We normally make tabs visible with ▶▸▸▸ but Go actually uses
" tabs and it's ugly as fuck, so this resets the tab listchars
" while still keeping trailing spaces visible.
set listchars=tab:\ \ ,trail:·
